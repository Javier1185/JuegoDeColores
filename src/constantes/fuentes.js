/**
 * fonts.js
 * -----------------------------------------------------------------------
 * Define qué archivos de fuente se deben cargar con expo-font antes
 * de mostrar la app (esto se usa en App.js con Font.loadAsync).
 *
 * IMPORTANTE: debes colocar los archivos .ttf reales en assets/fonts/.
 * Se recomienda la familia "Baloo 2" o "Fredoka" (Google Fonts),
 * ambas redondeadas, gruesas y muy usadas en apps infantiles.
 * Descárgalas gratis en: https://fonts.google.com/
 * -----------------------------------------------------------------------
 */

export const FONTS_TO_LOAD = {
  Baloo2: require('../../assets/fonts/Baloo2.ttf'),
};

export default FONTS_TO_LOAD;